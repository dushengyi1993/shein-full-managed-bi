# Local read-only dashboard
FULL_BI_HOST=127.0.0.1
FULL_BI_PORT=3100
FULL_BI_DATA_FILE=tests/fixtures/dashboard.json

# Reserved for the local PostgreSQL phase. Never commit a real value.
DATABASE_URL=
