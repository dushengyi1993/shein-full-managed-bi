# 销量查询权限包申请状态

核验时间：2026-07-20（Asia/Shanghai）

目标权限包：`销量查询`

对应接口：`/open-api/goods/query-sku-sales`

## 当前结果

18 家非 HL 全托应用均已先确认应用状态为“审核通过”，再提交销量查询权限包，提交后逐店回读均为“审核中”：

| 批次 | 店铺 | 回读状态 |
|---|---|---|
| 1 | QH、DL、DX、FY | 审核中 |
| 2 | LQ、NM、JY、ZL | 审核中 |
| 3 | TS、MZ、CX、YJ | 审核中 |
| 4 | XL、QY、TZ、JSH | 审核中 |
| 5 | TZZ、XC | 审核中 |

HL 未操作。

> 这张表记录的是 2026-07-20 的“历史应用申请”状态，不是当前店铺清单。后续已改为
> 这 18 个应用是历史记录。当前规范清单为 25 店、17 主体；原有 DL 多店授权保留作回滚，各店按公司主体应用重新授权，店铺代码采用“公司简称 + 账号后四位”。

每批操作完成后均按目标 Profile 精确关闭浏览器，并确认对应 CDP 端口已经释放。

## 后续验收边界

平台确认弹窗提示：订阅申请已提交后，需要开放平台审批。权限包显示“审核中”不代表接口已经可调用。

权限变为“已订阅”后，仍需依次完成：

1. 店铺级授权与凭证交换；
2. 单店只读权限探针；
3. `number-list` SKU 映射核对；
4. `query-sku-sales` 字段、缺失 SKU 和统计日核对；
5. 隔离数据层入仓与门户回读；
6. 首店稳定后再扩展到当前 25 店。
