[Unit]
# The new 100GB data disk owns large, regenerable or append-heavy runtime data.
# PostgreSQL and releases deliberately remain on the faster root disk.
RequiresMountsFor=/data /srv/shein-fm/runtime /srv/shein-fm/backups /srv/shein-fm/webapi/profiles
After=local-fs.target
